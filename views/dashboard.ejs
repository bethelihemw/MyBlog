<!DOCTYPE html>
<html lang="en">
<%- include('includes/head')%>
<body class="page dashboard-page">
    <%- include('includes/header')%>
    <main class="container">
        <% if(posts.length ==0 ){%> 
        <h4 class="page-subtitle"> Wellcome <%= user.username %> , you have no post</h4>
        <% } else {  %>
            <h4 class="page-subtitle">Wellcome <%= user.username %> , here are your posts</h4>
         <%}  %>

        <ul class="post-list">
            <% posts.forEach(post =>{ %>
                <li class="post-list__item"><a class="post-link" href="/post/<%= post.id %>"> 
                    <%= post.title %></a> <span class="post-list__date">on <%= new Date(post.createdDate).getMonth() + 1 %>
                     /<%= new Date(post.createdDate).getDate() %> /
                      <%= new Date(post.createdDate).getFullYear() %></span></li>
            <%}) %>
        </ul>
    </main>
    <%- include('includes/footer')%>
</body>
</html>