<!DOCTYPE html>
<html lang="en">
<%- include('includes/head')%>
<body class="page single-post-page">
    <!-- <header>
        <h2><a href="/">Our Wonderfull App</a></h2>
        <nav>
            <a href="/login">Login</a>
        </nav>
    </header> -->
    
    <%- include('includes/header')%>
    <main class="container">
        <div class="static">
            <small class="post-meta"><%= post.username %> on <%= new Date(post.createdDate).getMonth() + 1 %> /<%= new Date(post.createdDate).getDate() %> / <%= new Date(post.createdDate).getFullYear() %></small>
            <h1 class="post-title"> <%= post.title %></h1>
        </div>
        <article class="post">
            <div class="post-body">
                <%- filterUserHTML(post.body) %>
            </div>
        </article>

       <% if (isAuthor){ %>
        <a href="/edit-post/<%= post.id%>" class="button button--secondary"> Edit post</a>

       <form style="display: inline;" action="/delete-post/<%= post.id%>"  method="POST">
        <button class="button button--danger">Delete Post</button>
       </form>
      
       <%} %>
       
    </main>
    <%- include('includes/footer')%>
</body>
</html>