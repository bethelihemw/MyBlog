<!DOCTYPE html>
<html lang="en">
<%- include('includes/head')%>
<body>
    <header>
        <h2><a href="/">Our Wonderfull App</a></h2>
        <nav>
            <a href="/login">Login</a>
        </nav>
    </header>
    <main>
        
       <h1> <%= post.title %></h1>

       <article>
        <small>created by <%= post.username %> on <%= new Date(post.createdDate).getMonth() + 1 %> /<%= new Date(post.createdDate).getDate() %> / <%= new Date(post.createdDate).getFullYear() %></small>
        <div>
            <%= post.body %>
        </div>
       </article>
    </main>
    <%- include('includes/footer')%>
</body>
</html>